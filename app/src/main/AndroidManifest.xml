<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.material_design">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material_Design">

        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ListEmployeesActivity"
            android:label="Lista de Empleados" ></activity>

        <activity
            android:name=".MostrarActivity"
            android:label="Desarrollador" />

        <activity android:name=".AddEmployeeActivity"
            android:label="Agregar Empleado" />

        <activity android:name=".MainActivity"
            android:label="Guia 04 Material Design" ></activity>
    </application>

</manifest>